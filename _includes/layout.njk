<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-Working">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <style>
    body { font-family: sans-serif; max-width: 800px; margin: 2em auto; padding: 0 1em; }
    .breadcrumb { font-size: 1.2em; font-weight: bold; }
    .content-list { list-style: none; padding-left: 0; }
    .content-list li { font-size: 1.1em; margin-bottom: 0.5em; }
    
    .image-container {
      position: relative; /* This is the anchor for the download link */
      display: inline-block; /* Makes the container wrap the image */
    }

    .image-container img {
      display: block; /* Removes any weird spacing */
    }

    .download-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      
      /* Style the overlay */
      background: rgba(0, 0, 0, 0.5); /* Semi-transparent black */
      color: white;
      text-decoration: none;
      font-size: 2em;
      
      /* Center the icon */
      display: flex;
      align-items: center;
      justify-content: center;
      
      /* Hide it by default */
      opacity: 0;
      transition: opacity 0.2s ease;
    }

    .image-container:hover .download-overlay {
      opacity: 1; /* Show on hover */
    }
  </style>
</head>
<body>

  <header>
    <a href="/" class="breadcrumb">🏠 Home</a>

    {% if title and title != "Home" %}
      <span>/ {{ title }}</span>
    {% endif %}
    <hr>
  </header>

  <main>
    {{ content | safe }}
    <hr>
<p>
  <a href="/{{ page.inputPath }}" download>
    Download
  </a>
</p>
  </main>

</body>
</html>